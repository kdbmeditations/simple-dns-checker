package com.company.simplednsclient.utils;

import java.util.concurrent.TimeUnit;

public class NanoTo {
    public static final long SECOND = TimeUnit.NANOSECONDS.convert(1, TimeUnit.SECONDS);
    public static final long MILLISECOND = TimeUnit.NANOSECONDS.convert(1, TimeUnit.MILLISECONDS);
}
